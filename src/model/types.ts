export type GnbType = {
  list: {
    name: string;
  }[];
};

export type PromotionType = {
  list: {
    imgSrc: string;
    name: string;
    content: string;
  }[];
};

export type EventType = {
  list: {
    name: string;
    imgSrc: string;
  }[];
};
