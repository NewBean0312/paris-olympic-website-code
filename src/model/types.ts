export type GnbType = {
  list: {
    name: string;
  }[];
};

export type PromotionType = {
  list: {
    imgSrc: string;
    name: string;
    content: string;
  }[];
};

export type EventType = {
  list: {
    name: string;
    imgSrc: string;
  }[];
};

export type NewsType = {
  list: {
    no: string;
    dateTime: string;
    dateTime2: string;
    title: string;
  }[];
}
